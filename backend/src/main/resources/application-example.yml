server:
  port: 8080

spring:
  application:
    name: slms-backend
  datasource:
    url: jdbc:mysql://localhost:3306/slms?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: your_password_here
    driver-class-name: com.mysql.cj.jdbc.Driver
